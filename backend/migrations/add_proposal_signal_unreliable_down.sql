-- Down migration: remove 'signal_unreliable' proposal status (B2/B6 — Phase 1 of A1).
-- The status column is plain VARCHAR — no DDL change was made in the up migration.
-- This file exists as a placeholder / changelog marker — the value is enforced in application code.

-- (Intentionally empty — no DDL to reverse.)
